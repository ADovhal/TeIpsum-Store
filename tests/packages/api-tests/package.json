{
  "name": "@teipsum/api-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "newman": "newman run src/collections/smoke.json -e src/environments/prod.json -r cli,allure --reporter-allure-export reports/allure",
    "newman:smoke": "newman run src/collections/smoke.json -e src/environments/prod.json",
    "newman:reg": "newman run src/collections/regression.json -e src/environments/prod.json"
  },
  "devDependencies": {
    "newman": "^6.1.2",
    "newman-reporter-allure": "^2.0.0"
  }
}