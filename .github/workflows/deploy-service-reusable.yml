name: Reusable Deploy Service

on:
  workflow_call:
    inputs:
      SERVICE_NAME:
        required: true
        type: string
      DEPLOY_ENV:
        required: true
        type: string
      GIT_BRANCH:
        required: true
        type: string
      COMPOSE_FILE:
        required: true
        type: string
    secrets:
      PROJECT_DIR:
        required: true
      SSH_HOST:
        required: true
      SSH_USER:
        required: true
      SSH_KEY:
        required: true
      DEPLOY_PORT:
        required: true
      SERVICE_SERVER_PORT:
        required: true
      DB_NAME:
        required: true
      DB_URL:
        required: true
      DB_USER:
        required: true
      DB_PASSWORD:
        required: true
      SPRING_KAFKA_BOOTSTRAP_SERVERS:
        required: true
      JWT_SECRET:
        required: true
      JWT_REFRESH_SECRET:
        required: true
      CORS_ALLOWED_ORIGINS:
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy ${{ inputs.SERVICE_NAME }} to ${{ inputs.DEPLOY_ENV }}
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT }}
          script: |
            set -e
            
            echo "📦 Deploying service: ${{ inputs.SERVICE_NAME }}"
            PROJECT_DIR="${{ secrets.PROJECT_DIR }}"
            SERVICE_NAME="${{ inputs.SERVICE_NAME }}"
            SERVICE_DIR="$PROJECT_DIR/backend/$SERVICE_NAME"
            DEPLOY_ENV="${{ inputs.DEPLOY_ENV }}"
            COMPOSE_FILE="${{ inputs.COMPOSE_FILE }}"
            GIT_BRANCH="${{ inputs.GIT_BRANCH }}"

            TEMP_DIR=$(mktemp -d)
            echo "🛠 Created temp directory: $TEMP_DIR"

            echo "🔁 Cloning repository with precise file control..."
            git clone --branch "$GIT_BRANCH" --depth 1 \
              --filter=blob:none --no-checkout \
              https://github.com/${{ github.repository }} "$TEMP_DIR"
            
            cd "$TEMP_DIR"
            
            echo "⚙️ Setting up precise file checkout..."
            git sparse-checkout init --no-cone
            git sparse-checkout set \
              "backend/$SERVICE_NAME" \
              "backend/shared-dto/src" \
              "backend/shared-dto/pom.xml" \
              "backend/shared-dto/Dockerfile.dto" \
              "backend/pom.xml"
            
            git checkout "$GIT_BRANCH"
            
            echo "📤 Syncing files to remote project dir..."
            mkdir -p "$PROJECT_DIR/backend"
            rsync -av --delete "$TEMP_DIR/backend/$SERVICE_NAME/" "$PROJECT_DIR/backend/$SERVICE_NAME/"
            mkdir -p "$PROJECT_DIR/backend/shared-dto"
            rsync -av --delete "$TEMP_DIR/backend/shared-dto/src/" "$PROJECT_DIR/backend/shared-dto/src/"
            cp -v "$TEMP_DIR/backend/shared-dto/pom.xml" "$PROJECT_DIR/backend/shared-dto/pom.xml"
            cp -v "$TEMP_DIR/backend/shared-dto/Dockerfile.dto" "$PROJECT_DIR/backend/shared-dto/Dockerfile.dto"
            cp -v "$TEMP_DIR/backend/pom.xml" "$PROJECT_DIR/backend/pom.xml"

            echo "🔍 Verifying file structure..."
            test -f "$PROJECT_DIR/backend/pom.xml"
            test -f "$PROJECT_DIR/backend/shared-dto/pom.xml"

            echo "🧠 Check if shared-dto changed..."
            SHARED_DTO_DIR="$PROJECT_DIR/backend/shared-dto"
            SHARED_DTO_HASH_FILE="$SHARED_DTO_DIR/.build-hash"
            M2_CACHE_DIR="$PROJECT_DIR/backend/.m2"

            NEW_HASH=$(find "$SHARED_DTO_DIR/src" "$SHARED_DTO_DIR/pom.xml" -type f -exec sha256sum {} + | sort | sha256sum | cut -d" " -f1)
            echo "🔍 Shared DTO hash: $NEW_HASH"

            if [ ! -f "$SHARED_DTO_HASH_FILE" ] || [ "$NEW_HASH" != "$(cat $SHARED_DTO_HASH_FILE)" ]; then
              echo "🔨 Building shared-dto in Docker..."
              cd "$PROJECT_DIR/backend"
              docker build -f shared-dto/Dockerfile.dto -t shared-dto-builder .

              echo "📦 Extracting Maven .m2 repo..."
              docker create --name tmp-shared-dto shared-dto-builder
              mkdir -p "$M2_CACHE_DIR"
              docker cp tmp-shared-dto:/root/.m2 "$M2_CACHE_DIR"
              docker rm tmp-shared-dto

              echo "$NEW_HASH" > "$SHARED_DTO_HASH_FILE"
            else
              echo "✅ shared-dto is unchanged. Skipping build."
            fi

            echo "📁 Creating .env..."
            ENV_PATH="$SERVICE_DIR/.env"
            cat <<EOF > "$ENV_PATH"
            SERVICE_SERVER_PORT=${{ secrets.SERVICE_SERVER_PORT }}
            DB_NAME=${{ secrets.DB_NAME }}
            DB_URL=${{ secrets.DB_URL }}
            DB_USER=${{ secrets.DB_USER }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            JWT_REFRESH_SECRET=${{ secrets.JWT_REFRESH_SECRET }}
            CORS_ALLOWED_ORIGINS=${{ secrets.CORS_ALLOWED_ORIGINS }}
            SPRING_KAFKA_BOOTSTRAP_SERVERS=${{ secrets.SPRING_KAFKA_BOOTSTRAP_SERVERS }}
            EOF
            
            echo "🔄 Rebuilding Docker container..."
            cd "$PROJECT_DIR/backend"
            docker compose --env-file "$ENV_PATH" -f "$SERVICE_NAME/$COMPOSE_FILE" down --remove-orphans --volumes
            docker compose --env-file "$ENV_PATH" -f "$SERVICE_NAME/$COMPOSE_FILE" up -d --build

            echo "✅ Successfully deployed $SERVICE_NAME!"