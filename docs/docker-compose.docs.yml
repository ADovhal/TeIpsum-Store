services:
  teipsum-docs:
    container_name: teipsum-docs
    build: .
    ports:
      - "8888:8080"
    volumes:
      - ./specs:/usr/share/nginx/html/api-docs:ro
    networks:
      - prod-net

networks:
  prod-net:
    external: true